import { describe, it, expect } from 'vitest';

describe('client scaffold smoke', () => {
  it('basic math still works', () => {
    expect(1 + 1).toBe(2);
  });

  it('project name context', () => {
    // Placeholder — real component tests will come in later epics
    expect(true).toBe(true);
  });
});
